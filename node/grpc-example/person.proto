syntax = "proto3";

package person;

service Person {
    rpc greet (PersonRequest) returns (stream PersonResponse) {}
}

message PersonRequest {
    repeated string personNameList = 1;
}

message PersonResponse {
    string message = 1;
}