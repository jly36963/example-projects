# Next / Express / TS

A custom starting point for using Next (SSR), Express (API), and TS.

## Setup

- `npm i`

## Startup (dev)

- `npm run dev`

## Startup (prod)

- `npm run build`
- `npm run build:server`
- `npm start`
